# 阿里云服务器购买与系统部署详细教程

本教程面向「检测报告 Web 系统」（Spring Boot + React + PostgreSQL），从零完成：阿里云账号、域名购买、ECS 购买、域名解析、服务器环境搭建、应用部署与 Nginx 配置。

---

## 一、准备工作

### 1.1 需要准备的内容

- **身份证**：实名认证用  
- **手机号**：注册与验证  
- **支付宝或银行卡**：支付用（建议先充值 100–200 元）  
- **域名意向**：例如 `report.yourcompany.com`，先想好前缀和后缀（.com / .cn 等）

### 1.2 本系统部署架构简述

```
用户浏览器
    ↓ (HTTPS 443 / HTTP 80)
  Nginx（同一台 ECS）
    ↓ /api → 反向代理到 127.0.0.1:8080
  Spring Boot 后端 (8080)
    ↓
  PostgreSQL (5432，本机)
```

- **前端**：用 Vite 打成静态文件，由 Nginx 直接提供。  
- **后端**：jar 包运行在 8080，仅监听 127.0.0.1，由 Nginx 反向代理 `/api`。  
- **数据库**：PostgreSQL 安装在同一台 ECS 上（小型项目够用）。

---

## 费用说明：除了域名和服务器，还有哪些费用？

按本教程的部署方式（单机 ECS + 本机 PostgreSQL + Nginx），**主要支出就是域名和 ECS 两项**，其余多数可免费或按需产生费用。

| 项目 | 是否额外收费 | 说明 |
|------|--------------|------|
| **域名** | 收费 | 按年付，价格因后缀（.com / .cn 等）和首年/续费不同。 |
| **ECS 云服务器** | 收费 | 包年包月或按量付费，含所选配置的 CPU、内存、系统盘；**公网带宽/流量**通常在同一订单里选（按带宽或按流量计费），不算“单独产品”但会体现在 ECS 账单里。 |
| **公网带宽 / 流量** | 含在 ECS 内 | 买 ECS 时选的「按固定带宽」或「按使用流量」会算在 ECS 费用里，不会单独再买一次，但会拉高总价。 |
| **ICP 备案** | 免费 | 备案本身不向阿里云或管局交费。 |
| **SSL 证书（HTTPS）** | 可免费 | 阿里云「免费证书」、Let's Encrypt 等 DV 证书免费；若买 OV/EV 证书才收费。 |
| **数据库 RDS** | 本教程不用 | 教程里 PostgreSQL 装在 ECS 上，不买 RDS 则无此项费用；若以后改用云数据库 RDS，会单独计费。 |
| **对象存储 OSS** | 本教程不用 | 上传/签名目录在 ECS 本地磁盘，不接入 OSS 则无费用；若以后把文件存到 OSS，按存储与流量计费。 |
| **CDN** | 本教程不用 | 未使用 CDN 则无费用；若为加速静态资源接入 CDN，按流量/带宽计费。 |
| **负载均衡 SLB** | 本教程不用 | 单机部署不需要，无费用；多机部署时若用 SLB 会收费。 |
| **快照 / 备份** | 可选收费 | ECS 系统盘快照、自定义镜像等若开启并保留，会按容量计费；不创建则无。 |

**小结**：按教程做下来，**只需为「域名 + ECS（含所选带宽/流量）」付费**；HTTPS 用免费证书、备案不收费、数据库和文件都在本机，不产生额外产品费用。若以后用上 RDS、OSS、CDN、SLB 等，再单独计费。

---

## 二、阿里云账号注册与实名认证

### 2.1 注册账号

1. 打开阿里云官网：<https://www.aliyun.com>  
2. 点击右上角 **「免费注册」**  
3. 按提示填写：**手机号**、**验证码**、**登录密码**  
4. 完成注册后使用手机号或邮箱登录

### 2.2 实名认证（必须）

1. 登录后进入 **控制台**：<https://console.aliyun.com>  
2. 若未实名，页面会提示「实名认证」  
3. 点击 **「立即认证」**  
4. 选择 **「个人认证」** 或 **「企业认证」**  
   - **个人**：身份证正反面 + 人脸  
   - **企业**：营业执照 + 法人身份证等  
5. 提交后通常 **几分钟到 1 个工作日** 内完成  
6. 只有实名认证通过后，才能购买域名、ECS 等产品

---

## 三、购买域名

### 3.1 进入域名注册

1. 控制台搜索 **「域名」** 或进入 **「域名与网站」→「域名」**  
2. 或直接打开：<https://wanwang.aliyun.com/domain/>  
3. 在 **「域名查询」** 框输入你想用的名字，如 `myreport`，选后缀 `.com` / `.cn` 等，点击 **「查询」**

### 3.2 选择并购买域名

1. 在查询结果里会显示该域名是否 **「未注册」**  
2. 若未注册，点击 **「加入清单」**，再点 **「立即结算」**  
3. 确认 **「域名注册年限」**（建议至少 1 年）  
4. 勾选 **「域名隐私保护」**（可选，避免 whois 暴露个人邮箱/电话）  
5. 使用 **「阿里云账户余额」** 或 **「支付宝/银行卡」** 支付  
6. 支付成功后，在 **「域名列表」** 中可以看到该域名，状态为 **「正常」** 即可使用

### 3.3 域名备案说明（重要）

- **域名在中国大陆的服务器上用于访问网站，必须做 ICP 备案。**  
- 若你购买的 ECS 在 **中国大陆** 地域，且用该域名提供 Web 服务，需要：  
  1. 在阿里云控制台进入 **「ICP 备案」**  
  2. 按指引填写主体信息、网站信息、上传资料  
  3. 提交后由管局审核，通常 **约 1–2 周**  
- **若 ECS 选择「香港」或其他境外地域**，可以不备案直接解析使用，但访问速度与合规需自行权衡。  
- 本教程后续以「已备案域名」或「使用 IP 访问」为例，备案流程请在阿里云备案系统按提示操作。

---

## 四、购买 ECS 云服务器

### 4.1 进入 ECS 购买页

1. 控制台搜索 **「ECS」** 或 **「云服务器」**  
2. 点击 **「创建实例」** 或 **「立即购买」**  
3. 或打开：<https://ecs-buy.aliyun.com/>

### 4.2 基础配置（按步骤选择）

**（1）计费方式**

- **包年包月**：适合长期使用，价格更便宜，选 1 个月或 1 年  
- **按量付费**：按小时计费，不用可释放，适合测试

建议先选 **「包年包月」**，时长选 **1 个月** 试用。

**（2）地域与可用区**

- **地域**：选离你或主要用户近的，如 **华东1（杭州）**、**华北2（北京）**  
- 若不想备案，可选 **「中国香港」**  
- **可用区**：默认即可，多可用区可提高可用性（本教程单机部署选默认）

**（3）实例规格**

- 在 **「实例规格」** 里选 **「通用型」** 或 **「计算型」**  
- 入门推荐：**2 核 4 GiB**（如 ecs.c6.large 或同档位）  
- 若预算紧可先选 **1 核 2 GiB**，后续可升配

**（4）镜像**

- 选 **「公共镜像」**  
- **操作系统**：**Alibaba Cloud Linux 3** 或 **CentOS 7.9** 或 **Ubuntu 22.04**（本教程以 **Alibaba Cloud Linux 3** 为例，命令与 CentOS 类似）  
- 版本选当前推荐即可

**（5）存储**

- **系统盘**：默认 **40 GiB** 或 **50 GiB** 即可  
- 若需更大，可加 **数据盘**（后面再挂载格式化）

### 4.3 网络与安全组

**（1）网络**

- **专有网络 VPC**：默认会有一个，没有就「默认创建」  
- **公网 IP**：选择 **「分配公网 IPv4 地址」**，否则无法从外网访问  
- **带宽**：  
  - **按使用流量**：用多少算多少，适合访问量不稳定  
  - **按固定带宽**：如 1 Mbps、3 Mbps，适合小站  
- 入门可先 **1 Mbps 按固定带宽** 或 **按使用流量 100 M 峰值**

**（2）安全组**

- 选 **「新建安全组」**  
- 入方向规则建议先放开：  
  - **22**（SSH）  
  - **80**（HTTP）  
  - **443**（HTTPS）  
- 来源填 **0.0.0.0/0** 表示所有 IP（生产环境可再收紧）  
- 出方向默认「允许所有」即可

### 4.4 系统配置

**（1）登录凭证**

- **密钥对**：更安全，需在本地保存 .pem 私钥，SSH 时指定  
- **密码**：简单，自己设一个 **实例登录密码**（需符合长度和复杂度要求）  
- 首次建议选 **「自定义密码」**，记好密码

**（2）实例名称**

- 可填如 `report-web-prod`，方便在控制台识别

### 4.5 确认订单并支付

1. 勾选 **《云服务器 ECS 服务条款》**  
2. 点击 **「确认订单」**，核对费用  
3. 选择支付方式完成支付  
4. 支付成功后，在 **「实例列表」** 中可看到新实例，状态为 **「运行中」**  
5. 记下：**公网 IP**、**实例 ID**、**登录用户名**（root 或 ecs-user，镜像不同可能不同）

---

## 五、域名解析（把域名指到服务器）

### 5.1 打开域名解析

1. 控制台 **「域名」→「域名列表」**  
2. 找到刚买的域名，点击 **「解析」**

### 5.2 添加解析记录

1. 点击 **「添加记录」**  
2. 常用配置：  
   - **记录类型**：**A**  
   - **主机记录**：  
     - 用根域名访问填 **@**  
     - 用子域名如 `report.xxx.com` 填 **report**  
   - **记录值**：填你的 **ECS 公网 IP**  
   - **TTL**：默认 10 分钟即可  
3. 再点 **「确认」**  
4. 若要用 `www.xxx.com`，再添加一条：主机记录 **www**，记录类型 **A**，记录值同样为 ECS 公网 IP

### 5.3 生效时间

- 一般 **几分钟内** 生效，最多 48 小时  
- 本地可用 `ping 你的域名` 或 `nslookup 你的域名` 检查是否已解析到该 IP

---

## 六、连接服务器并做基础安全设置

### 6.1 使用 SSH 连接（Windows 示例）

**方式一：阿里云网页终端**

1. 在 ECS 实例列表点 **「远程连接」**  
2. 选 **「通过 Workbench 连接」**，输入实例密码即可

**方式二：本机 PowerShell / CMD**

```bash
ssh root@你的公网IP
# 或
ssh ecs-user@你的公网IP
```

按提示输入购买时设置的密码。

### 6.2 更新系统（可选但建议）

```bash
# Alibaba Cloud Linux / CentOS
sudo yum update -y

# Ubuntu
sudo apt update && sudo apt upgrade -y
```

### 6.3 防火墙（若有 firewalld）

```bash
# 查看
sudo firewall-cmd --list-all

# 开放 80、443、22
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --reload
```

若使用 **iptables** 或 **ufw**，需对应放行 22/80/443，此处不赘述。

---

## 七、在服务器上安装运行环境

以下以 **Alibaba Cloud Linux 3** 为例，路径与命令与 CentOS 类似。

### 7.1 安装 Java 17

```bash
sudo yum install -y java-17-openjdk java-17-openjdk-devel
java -version
# 应显示 openjdk 17.x
```

### 7.2 安装 PostgreSQL

```bash
sudo yum install -y postgresql15-server postgresql15

# 初始化并启动
sudo postgresql-setup --initdb
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 创建数据库和用户（按需改密码和库名）
sudo -u postgres psql -c "CREATE USER reportweb WITH PASSWORD '你的数据库密码';"
sudo -u postgres psql -c "CREATE DATABASE reportweb OWNER reportweb;"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE reportweb TO reportweb;"
```

若系统仓库没有 PostgreSQL 15，可先加官方 repo 再安装，或改用 12/14。

### 7.3 安装 Nginx

```bash
sudo yum install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

---

## 八、部署后端（Spring Boot）

### 8.1 在本地打包

在你本机项目根目录（有 `pom.xml` 的目录）：

```bash
mvn clean package -DskipTests -Pprod
```

如无 `prod` profile，可先只用：

```bash
mvn clean package -DskipTests
```

生成的 jar 一般在：`target/report-web-0.0.1-SNAPSHOT.jar`（或类似名称）。

### 8.2 上传 jar 到服务器

**方式一：本机 SCP（PowerShell 或 CMD）**

```bash
scp target/report-web-0.0.1-SNAPSHOT.jar root@你的公网IP:/opt/reportweb/
```

**方式二：先传到 OSS 再在服务器 wget**

在服务器上：

```bash
sudo mkdir -p /opt/reportweb
sudo chown 你的用户名:你的用户名 /opt/reportweb
# 再用 scp/sftp 把 jar 放到 /opt/reportweb/
```

### 8.3 创建目录与配置环境变量

在服务器上：

```bash
sudo mkdir -p /opt/reportweb/{uploads,signatures,logs}
sudo chown -R 你的用户名:你的用户名 /opt/reportweb
```

准备环境变量（建议用 systemd 或脚本，这里用脚本示例）：

```bash
# /opt/reportweb/env.sh（不要提交到代码库）
export DB_PASSWORD=你在上面设置的数据库密码
export JWT_SECRET=一串足够长的随机字符串
```

### 8.4 启动后端

```bash
cd /opt/reportweb
source /opt/reportweb/env.sh
java -jar -Dspring.profiles.active=prod report-web-0.0.1-SNAPSHOT.jar
```

若 8080 只监听 127.0.0.1，外网无法直连 8080，只能通过 Nginx 访问，这是预期行为。  
建议用 **systemd** 做开机自启与进程守护，例如：

```ini
# /etc/systemd/system/reportweb.service
[Unit]
Description=Report Web Backend
After=network.target postgresql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/reportweb
EnvironmentFile=/opt/reportweb/env.sh
ExecStart=/usr/bin/java -jar -Dspring.profiles.active=prod /opt/reportweb/report-web-0.0.1-SNAPSHOT.jar
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

然后：

```bash
sudo systemctl daemon-reload
sudo systemctl enable reportweb
sudo systemctl start reportweb
sudo systemctl status reportweb
```

---

## 九、部署前端（Vite 构建 + Nginx）

### 9.1 在本地构建前端

在项目里的 `frontend` 目录：

```bash
cd frontend
npm ci
npm run build
```

会在 `frontend/dist` 下生成静态文件。

### 9.2 上传到服务器

```bash
scp -r frontend/dist/* root@你的公网IP:/usr/share/nginx/html/report/
```

在服务器上先创建目录：

```bash
sudo mkdir -p /usr/share/nginx/html/report
# 再上传，或上传到 /tmp 后 sudo mv 到该目录
```

### 9.3 配置 Nginx（关键）

创建站点配置（域名换成你的）：

```bash
sudo vim /etc/nginx/conf.d/report.conf
```

内容示例（**请把 `your-domain.com` 改成你的域名或直接用服务器 IP 访问时的 default_server**）：

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    # 若暂时用 IP 访问，可写：server_name _;

    root /usr/share/nginx/html/report;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

检查配置并重载：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

此时：

- 访问 `http://你的域名` 或 `http://公网IP` 应能打开前端  
- 前端请求 `/api/...` 会被 Nginx 转到本机 8080，后端需已启动

---

## 十、HTTPS（可选但推荐）

### 10.1 使用阿里云免费 SSL 证书

1. 控制台搜索 **「SSL 证书」**  
2. **「免费证书」** → **「立即购买」**（个人型免费）  
3. 在 **「证书控制台」** 中 **「申请」** 证书，填写域名  
4. 按提示完成 **域名验证**（DNS 解析或文件验证）  
5. 签发后 **「下载」** 选择 **Nginx** 格式，得到 `.key` 和 `.pem`  
6. 上传到服务器，例如：`/etc/nginx/ssl/your-domain.key`、`/etc/nginx/ssl/your-domain.pem`  
7. 在 `report.conf` 里增加 443 并引用证书：

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com www.your-domain.com;
    ssl_certificate     /etc/nginx/ssl/your-domain.pem;
    ssl_certificate_key /etc/nginx/ssl/your-domain.key;
    # ... 其余 root、location 与上面 80 一致
}

server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

然后：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 10.2 使用 Let's Encrypt（可选）

若不想用阿里云证书，可在服务器装 **certbot** 自动申请与续期，步骤略，可查「certbot nginx 阿里云」教程。

---

## 十一、自检清单

- [ ] 阿里云账号已实名  
- [ ] 域名已购买并解析到 ECS 公网 IP  
- [ ] ECS 安全组已放行 22、80、443  
- [ ] 服务器已安装 Java 17、PostgreSQL、Nginx  
- [ ] 数据库已创建，`application-prod.yml` 中密码与 `DB_PASSWORD` 一致  
- [ ] 后端 jar 已上传并配置 `DB_PASSWORD`、`JWT_SECRET`，以 prod 模式运行  
- [ ] 前端已构建并上传到 Nginx 的 root 目录  
- [ ] Nginx 已配置 `/api/` 反向代理到 127.0.0.1:8080  
- [ ] 浏览器访问域名或 IP 能打开登录页，登录后接口正常  

---

## 十二、常见问题

**1. 无法 SSH 连接**  
- 检查安全组是否开放 22，本机网络是否有限制  
- 使用阿里云控制台「远程连接」测试是否为账号密码问题  

**2. 能打开页面但登录/接口 404 或 502**  
- 确认后端进程在运行：`systemctl status reportweb` 或 `ps aux | grep java`  
- 确认 Nginx 中 `proxy_pass` 为 `http://127.0.0.1:8080/api/;`（注意末尾斜杠）  
- 看 Nginx 错误日志：`sudo tail -f /var/log/nginx/error.log`  

**3. 数据库连接失败**  
- 确认 PostgreSQL 已启动，且 `application-prod.yml` 里 url、用户名、密码正确  
- 环境变量 `DB_PASSWORD` 是否在启动前 export 或通过 EnvironmentFile 注入  

**4. 上传/签名目录无权限**  
- 确认 `/opt/reportweb/uploads`、`/opt/reportweb/signatures` 所属用户与运行 Java 的用户一致  

**5. 域名已解析但无法访问**  
- 若在中国大陆且用域名访问网站，需完成 ICP 备案  
- 检查 Nginx `server_name` 是否包含该域名  

---

以上步骤完成后，你的系统即可通过域名（或 IP）在公网访问。若你提供当前使用的操作系统（如 Ubuntu 22.04）和是否必须备案，我可以再写一版「一键脚本」或更贴你环境的命令版本。
